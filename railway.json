{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx playwright install --with-deps chromium"
  },
  "deploy": {
    "startCommand": "node index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
